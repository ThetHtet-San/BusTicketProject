@model List<Com.MrIT.ViewModels.VmStaff>
@{
    ViewData["Title"] = "Listing";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Listing</h1>

<table class="table table-bordered table-striped">
    <thead>
        <tr>
            <th></th>
            <th>Code</th>
            <th>Name</th>
            <th>Address</th>
        </tr>
    </thead>
    <tbody>
        @{
            foreach (var item in Model)
            {
                <tr>
                    <td> <a href="@Url.Action("Edit", "Staff", new { a= System.Net.WebUtility.UrlEncode (item.EncryptId) })">Edit</a> | <a href="@Url.Action("Detail", "Staff", new { a= System.Net.WebUtility.UrlEncode (item.EncryptId) })">View</a></td>
                    <td> @item.Code  </td>
                    <td> @item.Name  </td>
                    <td> @item.Address  </td>
                </tr>
            }
        }
    </tbody>
    
</table>
